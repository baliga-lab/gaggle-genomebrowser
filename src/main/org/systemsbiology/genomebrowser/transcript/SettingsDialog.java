package org.systemsbiology.genomebrowser.transcript;

import javax.swing.*;

/**
 * Allow the user to select the tracks holding breaks and genes.
 */
@SuppressWarnings("serial")
public class SettingsDialog extends JDialog {
	
}
